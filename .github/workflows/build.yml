name: Build

# The APK is built here rather than by hand: GitHub's runners already carry the
# Android SDK, so this is the shortest path from a commit to something
# installable. Every push produces an artifact; a tag also produces a release.
on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  core:
    name: Core tests (JVM)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4

      # The protocol, library and API logic all live in :core, which is a plain
      # Kotlin/JVM module — so the part of the app most worth testing is tested
      # without an emulator or an Android SDK.
      - name: Run the core test suite
        timeout-minutes: 15
        run: ./gradlew :core:test --no-daemon --stacktrace

      - name: Check the wire format against node-roon-api
        run: |
          git clone --depth 1 https://github.com/RoonLabs/node-roon-api /tmp/node-roon-api
          git clone --depth 1 https://github.com/RoonLabs/node-roon-api-transport /tmp/node-roon-api-transport
          node tools/verify-wire.js /tmp/node-roon-api /tmp/node-roon-api-transport

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: core-test-report
          path: core/build/reports/tests/test

  apk:
    name: APK
    runs-on: ubuntu-latest
    needs: core
    # A build that hangs must fail with logs rather than sit until the six-hour
    # default expires: the logs are only retrievable once the job ends.
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # Install exactly what the build needs, here, where the download is
      # visible in the log — rather than leaving Gradle to fetch it mid-task
      # where a stall looks identical to a slow compile.
      - uses: android-actions/setup-android@v3
        with:
          packages: 'tools platform-tools platforms;android-36 build-tools;36.0.0'

      - uses: gradle/actions/setup-gradle@v4

      # The signing key, from a repository secret.
      #
      # It has to be the SAME key every build or Android will not install the
      # result over the copy already on the phone — the debug key this used
      # is generated per machine, so every runner produced a different
      # certificate and every "update" was refused.
      # A missing secret is NOT a broken build, and must not be reported as
      # one. Without the key the APK still compiles and every check still
      # runs — the only thing that cannot happen is PUBLISHING, because an
      # unsigned APK installs nowhere. So this records whether the key is
      # available and the publish step requires it; the job stays green and
      # starts publishing by itself once the secret exists.
      - name: Restore the signing key
        id: signing
        env:
          KEYSTORE_BASE64: ${{ secrets.MUSICD_KEYSTORE_BASE64 }}
        run: |
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "have_key=false" >> "$GITHUB_OUTPUT"
            echo "::warning::MUSICD_KEYSTORE_BASE64 is not set, so this build is" \
                 "unsigned and will not be published to dist/. Everything else ran." \
                 "See the Signing section of the README."
            exit 0
          fi
          printf '%s' "$KEYSTORE_BASE64" | base64 -d > "$RUNNER_TEMP/musicd-release.jks"
          echo "path=$RUNNER_TEMP/musicd-release.jks" >> "$GITHUB_OUTPUT"
          echo "have_key=true" >> "$GITHUB_OUTPUT"

      - name: Assemble the release APK
        timeout-minutes: 25
        env:
          MUSICD_KEYSTORE: ${{ steps.signing.outputs.path }}
          MUSICD_KEYSTORE_PASSWORD: ${{ secrets.MUSICD_KEYSTORE_PASSWORD }}
          MUSICD_KEY_ALIAS: musicd
        run: ./gradlew :app:assembleRelease --no-daemon --stacktrace

      # An unsigned APK installs nowhere, and one signed with the wrong key
      # installs but never updates. Neither may reach dist/, and the wrong-key
      # case is invisible until someone tries to update — so it is checked here
      # rather than discovered on a phone.
      - name: Check the APK is signed with the release key
        if: steps.signing.outputs.have_key == 'true'
        run: |
          apk=$(find app/build/outputs/apk/release -name '*.apk' | head -1)
          python3 tools/apk-cert.py "$apk" --expect-file tools/release-key.sha256

      # An unsigned build is named as such. Someone downloading a CI artifact
      # should not get a file that looks like a release and cannot install.
      - name: Name the artifact after the version
        id: name
        env:
          HAVE_KEY: ${{ steps.signing.outputs.have_key }}
        run: |
          apk=$(find app/build/outputs/apk/release -name '*.apk' | head -1)
          version=$(grep -oP 'versionName = "\K[^"]+' app/build.gradle.kts)
          if [ "$HAVE_KEY" = "true" ]; then
            out="musicd-remote-lite-${version}.apk"
          else
            out="musicd-remote-lite-${version}-UNSIGNED.apk"
          fi
          cp "$apk" "$out"
          echo "apk=$out" >> "$GITHUB_OUTPUT"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.name.outputs.apk }}
          path: ${{ steps.name.outputs.apk }}

      # Keep a directly downloadable APK in the repo, so the README can link
      # straight at it and sideloading is one tap rather than a trip through
      # the Actions UI.
      #
      # Committed once per versionName, not once per push: the APK is a build
      # output and git keeps every copy forever, so a 1.8 MB commit on every
      # push would bloat the history for nothing. Bumping versionName in
      # app/build.gradle.kts is what publishes a new one.
      #
      # This pushes with GITHUB_TOKEN, and GitHub deliberately does not fire
      # workflows for pushes made with it — so this cannot start a build loop.
      #
      # Requires the signing key: publishing an unsigned APK would put a file
      # in dist/ that the README links to and nobody can install.
      - name: Publish the APK into dist/
        if: >-
          github.event_name == 'push' && github.ref_type == 'branch'
          && steps.signing.outputs.have_key == 'true'
        env:
          APK: ${{ steps.name.outputs.apk }}
        run: |
          set -euo pipefail
          if [ -f "dist/$APK" ]; then
            echo "dist/$APK is already committed — nothing to publish."
            exit 0
          fi
          # One APK in the tree at a time. The old one is superseded rather
          # than kept: the README links to a single file, and git still has
          # every previous version in its history.
          #
          # mkdir comes AFTER this, not before: `git rm` of the last file in a
          # directory removes the directory too, so creating it first and
          # copying afterwards fails with "No such file or directory".
          git rm -q --ignore-unmatch 'dist/musicd-remote-lite-*.apk'
          mkdir -p dist
          cp "$APK" "dist/$APK"
          # The README's download link is rewritten here rather than by hand,
          # because a link that has to be remembered is a link that eventually
          # points at a version that no longer exists.
          # Every mention, not just the path: the link TEXT names the version
          # too, and a label that disagrees with what it downloads is worse
          # than no label.
          sed -i -E "s#musicd-remote-lite-[0-9][0-9A-Za-z._-]*\.apk#$APK#g" README.md
          git add "dist/$APK" README.md
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Publish $APK" -m "Built from ${GITHUB_SHA}."
          # The branch may have moved while this job ran.
          git pull --rebase origin "$GITHUB_REF_NAME"
          git push origin "HEAD:$GITHUB_REF_NAME"

      - name: Attach the APK to a tagged release
        if: >-
          startsWith(github.ref, 'refs/tags/v')
          && steps.signing.outputs.have_key == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.name.outputs.apk }}
